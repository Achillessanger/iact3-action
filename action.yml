name: 'Iact3 Action'
description: 'GitHub Action for Alibabacloud Ros Tool Iact3'
author: 'yunxiu'
inputs:
  access_key_id:
    description: 'ALIBABA_CLOUD_ACCESS_KEY_ID'
    required: true
  access_key_secret:
    description: 'ALIBABA_CLOUD_ACCESS_KEY_SECRET'
    required: true
  templates:
    description: 'test templates path'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'